
PID Controller
================

#set-point = 0<°C>
#current-temp = 60<°C>
#Kp = 0.1
#Ki = 0.1
#Kd = 0.1

#sim-timer = 1 ? t => <1s> => t + 1

#sim-timer => (#integral, #prev-error, #output) ? _ =>
  error = #set-point - #current-temp
  derivative = error - #prev-error
  control = #Kp * error + #Ki * #integral + #Kd * derivative 
  #prev-error := error

Interface
-----------

#keyboard.key => #set-point ?
  `+ => += 1<°C>
  `- => -= 1<°C>
  _ => _
